language: python
dist: bionic
python:
  - 3.6
  - 3.7
  - 3.8
install:
  - pip install poetry
  - poetry install
script:
  - ./manage.py migrate
  - PYTHONWARNINGS=always coverage run manage.py test
  - flake8 .
  - yapf -dr .
  - mypy .
  - safety check
after_success:
  - coveralls
